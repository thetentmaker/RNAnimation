import { StyleSheet, Text, View } from 'react-native';

const PlaylistFull = () => {
  return <Text style={{ color: 'white' }}>PlaylistFull</Text>;
};

export default PlaylistFull;

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: 'red',
  },
});
